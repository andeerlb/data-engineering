# Kafka Python Client (maintained fork with Python 3.14+ support)
kafka-python-ng==2.2.3

# Confluent Kafka Client with Avro support for Schema Registry integration
confluent-kafka[avro]==2.3.0

# Schema Registry client
# Note: confluent-kafka[avro] includes fastavro and schema-registry client

# HTTP requests for Schema Registry API examples
requests==2.31.0

python-dotenv==1.0.0
